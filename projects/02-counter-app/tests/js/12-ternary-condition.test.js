/* eslint-disable no-undef */
import { message } from '../../src/js/12-ternary-condition.js'

describe('Test on 12 ternary condition', () => {
  test('should return "active" when active is true', () => {
    expect(message).toBe('active')
  })
})
